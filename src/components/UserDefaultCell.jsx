const UserDefaultCell = ({ content }) => {
    return (
        <td className="hover:cursor-pointer hover:font-semibold py-[17px]">
            {content}
        </td>
    );
};

export default UserDefaultCell;
